# Hank
### A very, UNOPINIONATED dotfile synchronization and management tool.

There are a multitude of already existing (and well documented) dotfile synchronization tools. Many of them unfortunately require some manual setup, requiring that the user add/remove dotfile configs manually, or worst of all: requiring users to push their changes to a repository themselves.

With Hank, this should be done automatically or with very minimal effort.
